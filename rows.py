from row import Row
from collections import deque
class Rows(object):
	"""docstring for Rows"""
	def __init__(self, table):
		super(Rows, self).__init__()
		self.table = table
		self.db=table.db
		
		self.grud=deque()
	def __len__(self,clave):

	def __getitem__(self,clave):
		for row in self.grud:
			row[self]
		else:

		
	def __setitem__(self,clave,value):
		pass
	def insert(self,)

		